apiVersion: triggers.tekton.dev/v1alpha1
kind: EventListener
metadata:
  creationTimestamp: null
  name: cicd-event-listener
  namespace: sample-cicd
spec:
  serviceAccountName: pipeline
  triggers:
  - bindings:
    - name: github-pr-binding
    interceptors:
    - cel:
        filter: (header.match('X-GitHub-Event', 'pull_request') && body.action ==
          'opened' || body.action == 'synchronize') && body.pull_request.head.repo.full_name
          == 'gaganhegde/test-repo'
    - github:
        secretRef:
          namespace: sample-cicd
          secretKey: webhook-secret-key
          secretName: gitops-webhook-secret
    name: ci-dryrun-from-pr
    template:
      name: ci-dryrun-from-pr-template
  - bindings:
    - name: github-push-binding
    interceptors:
    - cel:
        filter: (header.match('X-GitHub-Event', 'push') && body.repository.full_name
          == 'gaganhegde/test-repo') && body.ref.startsWith('refs/heads/master')
    - github:
        secretRef:
          namespace: sample-cicd
          secretKey: webhook-secret-key
          secretName: gitops-webhook-secret
    name: cd-deploy-from-push
    template:
      name: cd-deploy-from-push-template
  - bindings:
    - name: sample-dev-taxi-binding
    - name: github-pr-binding
    interceptors:
    - cel:
        filter: (header.match('X-GitHub-Event', 'pull_request') && body.action ==
          'opened' || body.action == 'synchronize') && body.pull_request.head.repo.full_name
          == 'gaganhegde/taxi'
    - github:
        secretRef:
          namespace: sample-cicd
          secretKey: webhook-secret-key
          secretName: webhook-secret-sample-dev-taxi
    name: app-ci-build-from-pr-taxi
    template:
      name: app-ci-template
status:
  configuration:
    generatedName: ""
